/// This can be working with the latest version

struct Point {
    x: Int,
    y: Int,
}

let ps = [
    Point(0, 0),
    Point(1, 2),
    Point(2, 3),
];

enum Z {
    Z1,  // Z is this,
    Z2,  // xor this.
}

// Strings
let x = "hoge";
let y = "fuga";

// Numbers
let z: Nat = 0;

// Variants
// You can overwrite var names
let z = Z::Z2;

// Exporting this
{{
    points = ps,
    z = z,
    str = x + y,
}}
