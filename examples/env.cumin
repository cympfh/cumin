// OS Environ Values can read with ${},
// they are always Strings.
let user: String = ${USER};

// Cast to other types
let alpha = ${ALPHA} as Float;

// Default Value can be set.
// ${NAME:-DEFAULT}
let beta = ${BETA:-0.3} as Float;
