struct Person {
    name: string,
    sex: Sex,
    age: nat,
}

enum Sex {
    Male,
    Female,
    Other,
}

// list of Person
let names = [
    Person("John", Sex::Male, 17),
    Person { name="Xohn", sex=Sex::Other, age=1 },
];

// Cumin by
let author = Person {
    name = "cympfh",
    sex = Sex::Male,
    age = 0,
};
